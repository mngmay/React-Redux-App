const initialState = {
  movies: [],
  isLoading: false,
  error: ""
};

export const moviesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
